import { EqualFieldDirective } from './equal-field.directive';

describe('EqualFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualFieldDirective();
    expect(directive).toBeTruthy();
  });
});
